package com.amazonaws.services.connect.inappcalling.sample.data.domain.screenshare

// The screen share status
enum class ScreenShareStatus {

    // Local is sharing screen
    LOCAL,

    // Remote is sharing screen
    REMOTE,

    // No one is sharing screen
    NONE
}
